<%= form_for(movie) do |f| %>
  <% if movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_name %>
    <%= f.text_field :movie_name %>
  </div>

  <label>画像ファイルを指定:<%= f.file_field :movie_file, size: 50 %></label><br>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
